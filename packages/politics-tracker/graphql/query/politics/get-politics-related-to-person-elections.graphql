query GetPolticsRelatedToPersonElections($ids: [ID!]) {
  politics(where: {person: {id: {in: $ids}}}) {
    id
    desc
    source
    status
    reviewed
    createdAt
    updatedAt
    thread_parent {
      id
      desc
      source
			status
    }
    person {
      id
      election {
        id
      }
    }
    tag {
      id
      name
    }
  }
}