import { css } from 'styled-components'

export const sectionStyle = css`
  max-width: ${({ theme }) => theme.width.main};
  margin-left: auto;
  margin-right: auto;
  padding-left: 20px;
  padding-right: 20px;
`
