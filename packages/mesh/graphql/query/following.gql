query GetFollowingList($memberId: ID!) {
  member(where: { id: $memberId }) {
    name
    id
    followingCount
    follow_publisherCount
    follow_publisher {
      id
      customId
      logo
      title
    }
    following {
      id
      customId
      name
      avatar
    }
  }
}
