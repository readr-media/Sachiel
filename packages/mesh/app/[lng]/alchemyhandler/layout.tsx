export default function AlchemyHandlerLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}

// No metadata needed - this is an external callback handler
// No generateStaticParams needed - dynamic route only
